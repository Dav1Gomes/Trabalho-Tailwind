@page "/todos-restaurantes"
@inject NavigationManager NavigationManager

<div class="min-h-screen bg-white p-6 flex flex-col items-center">
    <h1 class="text-4xl font-bold mb-8 text-black text-center">Todos os Restaurantes</h1>

    <div class="grid grid-cols-1 md:grid-cols-2 gap-6 w-full max-w-5xl">
        @foreach (var restaurante in Restaurantes)
        {
            <div class="fade-in bg-green-200 border-2 border-[#6836c5] rounded-xl shadow-md p-6 cursor-pointer hover:scale-105 transform transition-transform duration-300" 
                 @onclick="@(() => NavigationManager.NavigateTo($"/restaurante/{restaurante.Id}"))">
                <h2 class="text-2xl font-bold text-black">@restaurante.Nome</h2>
                <p class="text-[#6836c5] mt-2">Nota: @restaurante.Nota ⭐</p>
            </div>
        }
    </div>
</div>

@code {
    private List<Restaurante> Restaurantes = new()
    {
        new Restaurante { Id = 1, Nome = "BurgerTop", Nota = 4.5 },
        new Restaurante { Id = 2, Nome = "PizzaBoom", Nota = 4.8 },
        new Restaurante { Id = 3, Nome = "SushiMax", Nota = 4.3 },
        new Restaurante { Id = 4, Nome = "PastaBella", Nota = 4.7 },
    };

    private class Restaurante
    {
        public int Id { get; set; }
        public string Nome { get; set; } = string.Empty;
        public double Nota { get; set; }
    }
}
